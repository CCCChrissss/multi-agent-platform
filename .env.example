# 複製成 .env 後填入實際值：cp .env.example .env
# 專案裡每個會連 DB / 呼叫 LLM 的模組都會自己 load_dotenv()，所以只要這一份就夠，
# 不用手動 export。

# --- 必填 ---

# Postgres 連線字串（checkpointer、call_log、event bus、run state、長期記憶都用它）。
# 刻意不叫 DATABASE_URL——那個名字被 LiteLLM Gateway 保留給它自己的 admin DB。
PERSISTENCE_DATABASE_URL=postgresql://localhost:5432/agent_architecture

# Anthropic API key。gateway/config.yaml 的 claude-haiku 靠它：台積電場景的
# stt / notified，以及除外責任場景的 check / notified 都會用到。
# 目前兩個示範場景都需要它。
ANTHROPIC_API_KEY=

# --- 視 workflow / 工具而定 ---

# Gemini API key。除外責任場景的 stt 宣告為 gemini-cheap，因此執行
# stt_exclusion_notify 時必填。以下工具也會使用：
#   gemini-cheap  -> scripts/distill_procedural.py 的知識蒸餾
#   gemini-strong -> evals/run_eval.py --model 的對照診斷
GEMINI_API_KEY=

# 要跑哪一份 workflow 定義檔。不填預設 workflows/definitions/stt_check_notify.yaml
# （台積電場景）。改這個要重啟 honcho 兩批 process，見 README「切換示範 workflow」。
# WORKFLOW_DEF_PATH=workflows/definitions/stt_exclusion_notify.yaml

# event bus backend，目前只實作 postgres（Kafka 見 GitHub Issues #8）。
# EVENT_BUS_BACKEND=postgres
