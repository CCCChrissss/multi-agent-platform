# Legacy Claude Code compatibility note

Project instructions now live in [AGENTS.md](AGENTS.md). Read that file before
changing this repository.

`CLAUDE.md` remains only so older Claude Code-based workflows do not silently
lose the project context. Do not duplicate or maintain a second set of rules
here; `AGENTS.md` is the source of truth.
