# Windows / PowerShell：只在 .env 不存在時複製：
# if (-not (Test-Path -LiteralPath '.env')) { Copy-Item -LiteralPath '.env.example' -Destination '.env' }
# macOS / Bash 上游參考：cp .env.example .env
# 專案裡每個會連 DB / 呼叫 LLM 的模組都會自己 load_dotenv()，所以只要這一份就夠，
# 不用把 secret 寫進 shell 設定。不要提交 .env。

# --- 必填 ---

# Postgres 連線字串（checkpointer、call_log、event bus、run state、長期記憶都用它）。
# 刻意不叫 DATABASE_URL——那個名字被 LiteLLM Gateway 保留給它自己的 admin DB。
PERSISTENCE_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/agent_architecture_test

# 目前 stt_check_notify 的 stt / check / notified 都宣告 gemini-cheap，
# 因此執行該 workflow 需要有效的 Gemini key。
# Anthropic API key 只供仍宣告 claude-haiku 的 workflow / 工具使用；目前
# stt_exclusion_notify 的 check / notified 需要它。沒有 key 時保持空白，
# 不要填假 credential。
ANTHROPIC_API_KEY=

# --- 視 workflow / 工具而定 ---

# Gemini API key。stt_exclusion_notify 的 stt 仍宣告為 gemini-cheap，因此執行
# 該 workflow 時需要有效 key。以下工具也會使用：
#   gemini-cheap  -> scripts/distill_procedural.py 的知識蒸餾
#   gemini-strong -> evals/run_eval.py --model 的對照診斷
GEMINI_API_KEY=
# 要跑哪一份 workflow 定義檔。不填時，Agent Runtime 與 workers 預設使用
# workflows/definitions/stt_check_notify.yaml（台積電場景）。改這個要重啟
# Procfile 與 Procfile.workers 兩批 process；trigger 的 --workflow-def 不會替
# 已啟動的 Runtime / workers 熱切換。
# WORKFLOW_DEF_PATH=workflows/definitions/stt_exclusion_notify.yaml

# event bus backend，目前只實作 postgres（Kafka 見 GitHub Issues #8）。
# EVENT_BUS_BACKEND=postgres

# Breeze-ASR-25 / Hugging Face Hub 快取。Windows 建議指向 D 槽，避免約 3.1 GB
# 模型權重與後續版本快取佔用 C 槽。請依自己的 workspace 路徑調整。
HF_HUB_CACHE=D:\Projects\multi-agent平台架設\.hf-cache
